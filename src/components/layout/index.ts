/**
 * 布局相关组件导出文件
 * @description 统一导出布局相关组件，方便统一导入
 */

import AppHeader from './AppHeader.vue';
import AppLayout from './AppLayout.vue';
import AppPlayer from './AppPlayer.vue';
import AppSider from './AppSider.vue';

export {
  AppHeader,
  AppLayout,
  AppPlayer,
  AppSider
};

/**
 * 默认导出所有布局相关组件
 */
export default {
  AppHeader,
  AppLayout,
  AppPlayer,
  AppSider
};